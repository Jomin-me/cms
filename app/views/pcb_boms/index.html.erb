<% content_for(:nav) { %>
    <%= render :partial => 'layouts/cside' %>
<% } %>
<% content_for(:page_header) { %>
    <h5>PCB BOMS</h5>
<% } %>

<table class="table table-striped table-condensed table-bordered">
  <thead>
  <tr>
    <th>PCB名</th>
    <th>操作</th>
  </tr>
  </thead>
  <tbody>
  <% @pcb_boms.each do |pcb_bom| %>
      <tr>
        <td><%= pcb_bom.name %></td>
        <td>
          <%= link_to '查看&编辑', edit_pcb_bom_path(pcb_bom.name), class: 'btn btn-mini' %>
        </td>
      </tr>
  <% end %>
  </tbody>
</table>

